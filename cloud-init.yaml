#cloud-config
package_update: true
packages:
  - apache2

runcmd:
  - systemctl enable apache2
  - systemctl restart apache2
  - [ bash, -c, "echo \"<html><body><h1>Running from $(hostname)</h1></body></html>\" > /var/www/html/index.html" ]